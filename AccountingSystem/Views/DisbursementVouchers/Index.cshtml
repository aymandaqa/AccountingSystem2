@model IEnumerable<AccountingSystem.Models.DisbursementVoucher>
@{
    ViewData["Title"] = "سندات   دفع  ";
}

<div class="container-fluid">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-end gap-2 mb-3">
        <a asp-action="Create" class="btn btn-primary" asp-require-permission="disbursementvouchers.create">سند دفع جديد</a>
        <a asp-action="ExportExcel"
           class="btn btn-success"
           asp-route-fromDate="@ViewBag.FromDate"
           asp-route-toDate="@ViewBag.ToDate"
           asp-require-permission="disbursementvouchers.view">تصدير Excel</a>
    </div>

    <form method="get" class="row g-3 mb-3">
        <div class="col-md-3">
            <label for="fromDate" class="form-label">من تاريخ</label>
            <input type="date" id="fromDate" name="fromDate" class="form-control" value="@ViewBag.FromDate" />
        </div>
        <div class="col-md-3">
            <label for="toDate" class="form-label">إلى تاريخ</label>
            <input type="date" id="toDate" name="toDate" class="form-control" value="@ViewBag.ToDate" />
        </div>
        <div class="col-md-6 d-flex align-items-end gap-2 justify-content-md-end">
            <button type="submit" class="btn btn-secondary">بحث</button>
            <a asp-action="Index" class="btn btn-outline-secondary">مسح</a>
        </div>
    </form>

    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>التاريخ</th>
                <th>المورد</th>
                <th>العملة</th>
                <th>سعر الصرف</th>
                <th>المبلغ</th>
                <th>الفرع</th>
                <th class="text-end">إجراءات</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var v in Model)
            {
                <tr>
                    <td>@v.Date.ToString("dd/MM/yyyy")</td>
                    <td>@v.Supplier.NameAr</td>
                    <td>@v.Currency.Code</td>
                    <td>@v.ExchangeRate.ToString("N6")</td>
                    <td>@v.Amount.ToString("N2")</td>
                    <td>@(v.CreatedBy?.PaymentBranch?.NameAr ?? "-")</td>
                    <td class="text-end">
                        <form asp-action="Delete" asp-route-id="@v.Id" method="post" class="d-inline" asp-require-permission="disbursementvouchers.delete" onsubmit="return confirm('هل أنت متأكد من حذف سند الدفع؟');">
                            <input type="hidden" name="fromDate" value="@ViewBag.FromDate" />
                            <input type="hidden" name="toDate" value="@ViewBag.ToDate" />
                            <button type="submit" class="btn btn-sm btn-danger">حذف</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
