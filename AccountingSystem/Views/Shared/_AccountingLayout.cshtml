@using AccountingSystem.Models
@using AccountingSystem.ViewComponents
@using AccountingSystem.ViewModels.Dashboard
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Identity
@using Microsoft.EntityFrameworkCore
@using System.Globalization
@using System.Linq
@using System.Text.Json
@inject IAuthorizationService AuthorizationService
@inject UserManager<User> UserManager
@inject AccountingSystem.Data.ApplicationDbContext DbContext
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - RoadFinity - ERP System</title>

    <!-- Bootstrap RTL -->
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <!-- Site styles -->
    <link rel="stylesheet" href="~/AccountingSystem.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/accounting.css" asp-append-version="true" />
    <link href="https://cdn.syncfusion.com/ej2/31.1.21/bootstrap5.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />

    <script src="https://cdn.syncfusion.com/ej2/31.1.21/dist/ej2.min.js"></script>

    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.0/dist/jquery.min.js"></script>
    <script>
        if (!window.jQuery) {
            var fallbackScript = document.createElement('script');
            fallbackScript.src = '@Url.Content("~/lib/jquery/dist/jquery.min.js")';
            document.head.appendChild(fallbackScript);
        }
    </script>

    @await RenderSectionAsync("Styles", required: false)
    <Style>
        .container, .container-fluid, .container-lg, .container-md, .container-sm, .container-xl, .container-xxl {
            --bs-gutter-x: 0.5rem;
        }

        #sidebarMenu .nav-link {
            cursor: grab;
        }

            #sidebarMenu .nav-link:active {
                cursor: grabbing;
            }

        #sidebarMenu .sortable-ghost {
            opacity: 0.4;
        }

        #sidebarMenu .sortable-chosen {
            background-color: rgba(255, 255, 255, 0.05);
        }

        .nav-link {
            color: #e2e6eb;
        }
    </Style>
</head>
<body>

    <ejs-toast id="toast_type" created="alertcreated" close="alertclose">
        <e-toast-position X="Left" Y="Bottom"></e-toast-position>
    </ejs-toast>


    @{
        var canBusinessStatementBulk = (await AuthorizationService.AuthorizeAsync(User, "accountmanagement.businessstatementbulk")).Succeeded;
        var canBusnissStatment = (await AuthorizationService.AuthorizeAsync(User, "accountmanagement.busnissstatment")).Succeeded;
        var canDriverPayment = (await AuthorizationService.AuthorizeAsync(User, "accountmanagement.driverpayment")).Succeeded;
        var canDriverStatment = (await AuthorizationService.AuthorizeAsync(User, "accountmanagement.driverstatment")).Succeeded;
        var canUserPayment = (await AuthorizationService.AuthorizeAsync(User, "accountmanagement.userpayment")).Succeeded;
        var canCurrencies = (await AuthorizationService.AuthorizeAsync(User, "currencies.view")).Succeeded;
        var canSuppliers = (await AuthorizationService.AuthorizeAsync(User, "suppliers.view")).Succeeded;
        var canSupplierTypes = (await AuthorizationService.AuthorizeAsync(User, "suppliertypes.view")).Succeeded;
        var showSuppliers = canSuppliers || canSupplierTypes;
        var canAgents = (await AuthorizationService.AuthorizeAsync(User, "agents.view")).Succeeded;
        var canSystemSettings = (await AuthorizationService.AuthorizeAsync(User, "systemsettings.view")).Succeeded;
        var canPermissionGroups = (await AuthorizationService.AuthorizeAsync(User, "permissiongroups.view")).Succeeded;
        var canReceiptVouchers = (await AuthorizationService.AuthorizeAsync(User, "receiptvouchers.view")).Succeeded;
        var canDisbursementVouchers = (await AuthorizationService.AuthorizeAsync(User, "disbursementvouchers.view")).Succeeded;
        var canPaymentVouchers = (await AuthorizationService.AuthorizeAsync(User, "paymentvouchers.view")).Succeeded;
        var canWorkflowApprovalsView = (await AuthorizationService.AuthorizeAsync(User, "workflowapprovals.view")).Succeeded;
        var canWorkflowDefinitionsManage = (await AuthorizationService.AuthorizeAsync(User, "workflowdefinitions.manage")).Succeeded;
        var canReceiptVouchersCreate = (await AuthorizationService.AuthorizeAsync(User, "receiptvouchers.create")).Succeeded;
        var canDisbursementVouchersCreate = (await AuthorizationService.AuthorizeAsync(User, "disbursementvouchers.create")).Succeeded;
        var canPaymentVouchersCreate = (await AuthorizationService.AuthorizeAsync(User, "paymentvouchers.create")).Succeeded;
        var canDynamicScreensManage = (await AuthorizationService.AuthorizeAsync(User, "dynamicscreens.manage")).Succeeded;
        var showAccountManagement = canBusinessStatementBulk || canBusnissStatment || canDriverPayment || canDriverStatment || canUserPayment;
        var canAssets = (await AuthorizationService.AuthorizeAsync(User, "assets.view")).Succeeded;
        var canAssetTypes = (await AuthorizationService.AuthorizeAsync(User, "assettypes.view")).Succeeded;
        var canAssetExpenses = (await AuthorizationService.AuthorizeAsync(User, "assetexpenses.view")).Succeeded;
        var showVouchers = canReceiptVouchers || canDisbursementVouchers || canPaymentVouchers || canWorkflowApprovalsView || canWorkflowDefinitionsManage || canReceiptVouchersCreate || canDisbursementVouchersCreate || canPaymentVouchersCreate || canAssetExpenses;
        var showAssets = canAssets || canAssetExpenses || canAssetTypes;
        var canTransactionsOverview = canReceiptVouchers || canDisbursementVouchers || canPaymentVouchers || canAssetExpenses;

        var canDashboardView = (await AuthorizationService.AuthorizeAsync(User, "dashboard.view")).Succeeded;

        var canAccountsView = (await AuthorizationService.AuthorizeAsync(User, "accounts.view")).Succeeded;
        var canAccountsCreate = (await AuthorizationService.AuthorizeAsync(User, "accounts.create")).Succeeded;
        var canAccountsEdit = (await AuthorizationService.AuthorizeAsync(User, "accounts.edit")).Succeeded;
        var showAccounts = canAccountsView || canAccountsCreate || canAccountsEdit;

        var canCashClosuresView = (await AuthorizationService.AuthorizeAsync(User, "cashclosures.view")).Succeeded;
        var canCashClosuresCreate = (await AuthorizationService.AuthorizeAsync(User, "cashclosures.create")).Succeeded;
        var canCashClosuresApprove = (await AuthorizationService.AuthorizeAsync(User, "cashclosures.approve")).Succeeded;
        var canCashClosuresReport = (await AuthorizationService.AuthorizeAsync(User, "cashclosures.report")).Succeeded;
        var canTransfersView = (await AuthorizationService.AuthorizeAsync(User, "transfers.view")).Succeeded;
        var canTransfersManage = (await AuthorizationService.AuthorizeAsync(User, "transfers.manage")).Succeeded;
        var canTransfersApprove = (await AuthorizationService.AuthorizeAsync(User, "transfers.approve")).Succeeded;
        var showOperations = canCashClosuresView || canCashClosuresCreate || canCashClosuresApprove || canCashClosuresReport || canTransfersView || canTransfersManage;

        var canJournalView = (await AuthorizationService.AuthorizeAsync(User, "journal.view")).Succeeded;
        var canJournalCreate = (await AuthorizationService.AuthorizeAsync(User, "journal.create")).Succeeded;
        var canJournalApprove = (await AuthorizationService.AuthorizeAsync(User, "journal.approve")).Succeeded;
        var showJournal = canJournalView || canJournalCreate || canJournalApprove;

        var canReportsView = (await AuthorizationService.AuthorizeAsync(User, "reports.view")).Succeeded;
        var canReportsPending = (await AuthorizationService.AuthorizeAsync(User, "reports.pending")).Succeeded;
        var canReportsDynamic = (await AuthorizationService.AuthorizeAsync(User, "reports.dynamic")).Succeeded;
        var canQuickAccountStatementShortcut = (await AuthorizationService.AuthorizeAsync(User, "reports.quickaccountstatement")).Succeeded;
        var showReports = canReportsView || canReportsPending || canReportsDynamic;

        var hasCashBox = false;
        if (User.Identity?.IsAuthenticated ?? false)
        {
            var currentUser = await UserManager.GetUserAsync(User);
            if (currentUser != null)
            {
                hasCashBox = currentUser.PaymentAccountId.HasValue;
                if (!hasCashBox)
                {
                    hasCashBox = await DbContext.UserPaymentAccounts
                    .AsNoTracking()
                    .AnyAsync(upa => upa.UserId == currentUser.Id);
                }
            }
        }

        var isSuperAdmin = User.IsInRole("SuperAdmin") || User.IsInRole("Admin");
        var showQuickAccountStatement = (isSuperAdmin && canReportsView) || canQuickAccountStatementShortcut;

        var canBranches = (await AuthorizationService.AuthorizeAsync(User, "branches.view")).Succeeded;
        var canCostCenters = (await AuthorizationService.AuthorizeAsync(User, "costcenters.view")).Succeeded;
        var canUsers = (await AuthorizationService.AuthorizeAsync(User, "users.view")).Succeeded;
        var showSettings = canBranches || canCostCenters || canCurrencies || canPermissionGroups || canUsers;

        var canEmployeesView = (await AuthorizationService.AuthorizeAsync(User, "employees.view")).Succeeded;
        var canPayrollView = (await AuthorizationService.AuthorizeAsync(User, "payroll.view")).Succeeded;
        var canPayrollProcess = (await AuthorizationService.AuthorizeAsync(User, "payroll.process")).Succeeded;
        var canSalaryPaymentsView = (await AuthorizationService.AuthorizeAsync(User, "salarypayments.view")).Succeeded;
        var canEmployeeAdvancesView = (await AuthorizationService.AuthorizeAsync(User, "employeeadvances.view")).Succeeded;
        var canEmployeesEdit = (await AuthorizationService.AuthorizeAsync(User, "employees.edit")).Succeeded;
        var showHr = canEmployeesView || canPayrollView || canSalaryPaymentsView || canEmployeeAdvancesView || canPayrollProcess || canEmployeesEdit;

        var dashboardModel = ViewData.Model as CashPerformanceDashboardViewModel;
        var pendingIncomingTransfers = dashboardModel?.PendingIncomingTransfers ?? Enumerable.Empty<PaymentTransfer>();
        var hasPendingIncomingTransfers = dashboardModel?.HasPendingIncomingTransfers == true;
        var incomingTransfersPermission = canTransfersManage ? "transfers.manage" : (canTransfersApprove ? "transfers.approve" : string.Empty);
        var transferReturnUrl = Url.Action("Index", "Dashboard");

        var currentController = ViewContext.RouteData.Values["controller"]?.ToString()?.ToLower();
        var currentAction = ViewContext.RouteData.Values["action"]?.ToString()?.ToLower();
        var accountsActive = currentController == "accounts";
        var suppliersActive = currentController == "suppliers" || currentController == "suppliertypes";
        var operationsActive = currentController == "cashboxclosures" || currentController == "expenses" || currentController == "transfers";
        var vouchersActive = currentController == "receiptvouchers" || currentController == "disbursementvouchers" || currentController == "paymentvouchers" || currentController == "workflowapprovals" || currentController == "workflowdefinitions" || currentController == "transactions" || currentController == "accountsettlements";
        var accountMgmtActive = currentController == "accountmanagement";
        var journalActive = currentController == "journalentries";
        var reportsActive = currentController == "reports" || currentController == "dynamicrdlcreports";
        var settingsActive = currentController == "branches" || currentController == "costcenters" || currentController == "currencies" || currentController == "users" || currentController == "permissiongroups";
        var hrActive = currentController == "employees" || currentController == "payrolls" || currentController == "deductiontypes" || currentController == "employeedeductions" || currentController == "employeeloans";
        var assetsActive = currentController == "assets" || currentController == "assetexpenses" || currentController == "assettypes" || currentController == "assetdepreciations";
    }

    @if (hasPendingIncomingTransfers)
    {
        <div class="modal fade" id="incomingTransfersModal" tabindex="-1" aria-labelledby="incomingTransfersLabel" aria-hidden="true">
            <div class="modal-dialog modal-lg modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header bg-warning-subtle">
                        <h5 class="modal-title" id="incomingTransfersLabel">حوالات واردة بانتظار المعالجة</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <p class="mb-4">يبدو أن لديك حوالات واردة قيد الانتظار. يرجى مراجعتها واستلامها لإغلاقها.</p>

                        <div class="table-responsive">
                            <table class="table table-hover align-middle mb-0">
                                <thead class="table-light">
                                    <tr>
                                        <th scope="col">المرسل</th>
                                        <th scope="col">الفرع المرسل</th>
                                        <th scope="col" class="text-end">المبلغ</th>
                                        <th scope="col">تاريخ الإنشاء</th>
                                        <th scope="col" class="text-end">الإجراء</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var transfer in pendingIncomingTransfers)
                                    {
                                        <tr>
                                            <td class="fw-semibold">@(transfer.Sender?.FullName ?? transfer.SenderId)</td>
                                            <td>@(transfer.FromBranch?.NameAr ?? "-")</td>
                                            <td class="text-end">@transfer.Amount.ToString("N2", CultureInfo.InvariantCulture)</td>
                                            <td class="text-nowrap">@transfer.CreatedAt.ToString("dd/MM/yyyy HH:mm")</td>
                                            <td class="text-end">
                                                <a class="btn btn-primary btn-sm"
                                                   asp-controller="Transfers"
                                                   asp-action="Approve"
                                                   asp-route-id="@transfer.Id"
                                                   asp-route-accept="true"
                                                   asp-route-returnUrl="@transferReturnUrl"
                                                   asp-require-permission="@incomingTransfersPermission">
                                                    <i class="fa fa-check ms-1"></i> استلام الحوالة
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">تجاهل</button>
                    </div>
                </div>
            </div>
        </div>
    }
    <div id="wrapper" class="d-flex">
        <nav id="sidebar" class="d-flex flex-column">
            <div class="sidebar-header p-3 mb-3 border-bottom">
                <a class="navbar-brand d-flex align-items-center text-white" href="@Url.Action("Index", "Dashboard")">
                    <i class="fas fa-calculator me-2"></i> RoadFinity ERP System
                </a>
            </div>
            <div class="sidebar-search px-3 mb-3">
                <div class="input-group input-group-sm">
                    <span class="input-group-text"><i class="fas fa-search"></i></span>
                    <input type="search" id="menuSearch" class="form-control" placeholder="ابحث في القائمة..." aria-label="بحث في القائمة">
                </div>
            </div>
            <div class="sidebar-menu flex-grow-1 overflow-auto px-2">
                <ul class="nav flex-column" id="sidebarMenu">
                    <li class="nav-item" data-menu-key="dashboard">
                        <a class="nav-link @(currentController == "dashboard" && currentAction == "index" ? "active" : "")"
                           asp-controller="Dashboard"
                           asp-action="Index"
                           asp-require-permission="dashboard.view">
                            <i class="fas fa-tachometer-alt"></i> لوحة التحكم
                        </a>
                    </li>
                    <li class="nav-item" data-menu-key="company-performance">
                        <a class="nav-link @(currentController == "home" && currentAction == "index" ? "active" : "")"
                           asp-controller="Home"
                           asp-action="Index"
                           asp-require-permission="dashboard.companyperformance">
                            <i class="fas fa-tachometer-alt"></i> عرض اداء الشركة
                        </a>
                    </li>
                    <li class="nav-item" data-menu-key="accounts">
                        <a class="nav-link nav-link-toggle d-flex align-items-center justify-content-between" data-bs-toggle="collapse" href="#accountsMenu" data-bs-target="#accountsMenu" role="button" aria-expanded="@(accountsActive ? "true" : "false")" aria-controls="accountsMenu">
                            <span class="nav-link-label"><i class="fas fa-list-alt"></i> الحسابات</span>
                            <i class="fas fa-angle-down toggle-icon"></i>
                        </a>
                        <ul class="submenu collapse nav flex-column @(accountsActive ? "show" : "")" id="accountsMenu">
                            <li class="nav-item">
                                <a class="nav-link"
                                   asp-controller="Accounts"
                                   asp-action="Tree"
                                   asp-require-permission="accounts.view">
                                    <i class="fas fa-sitemap"></i> شجرة الحسابات
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link"
                                   asp-controller="Accounts"
                                   asp-action="ManageTree"
                                   asp-require-permission="accounts.edit">
                                    <i class="fas fa-diagram-project"></i> إدارة شجرة الحسابات
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link"
                                   asp-controller="Accounts"
                                   asp-action="Index"
                                   asp-require-permission="accounts.view">
                                    <i class="fas fa-list"></i> قائمة الحسابات
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link"
                                   asp-controller="Accounts"
                                   asp-action="Create"
                                   asp-require-permission="accounts.create">
                                    <i class="fas fa-plus"></i> إضافة حساب جديد
                                </a>
                            </li>
                        </ul>
                    </li>
                    @if (canAgents)
                    {
                        <li class="nav-item" data-menu-key="agents">
                            <a class="nav-link @(currentController == "agents" ? "active" : "")"
                               asp-controller="Agents"
                               asp-action="Index"
                               asp-require-permission="agents.view">
                                <i class="fas fa-user-tie"></i> الوكلاء
                            </a>
                        </li>
                    }
                    @if (showSuppliers)
                    {
                        <li class="nav-item" data-menu-key="suppliers">
                            <a class="nav-link nav-link-toggle d-flex align-items-center justify-content-between" data-bs-toggle="collapse" href="#suppliersMenu" data-bs-target="#suppliersMenu" role="button" aria-expanded="@(suppliersActive ? "true" : "false")" aria-controls="suppliersMenu">
                                <span class="nav-link-label"><i class="fas fa-truck"></i> إدارة الموردين</span>
                                <i class="fas fa-angle-down toggle-icon"></i>
                            </a>
                            <ul class="submenu collapse nav flex-column @(suppliersActive ? "show" : "")" id="suppliersMenu">
                                @if (canSuppliers)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link @(currentController == "suppliers" ? "active" : "")"
                                           asp-controller="Suppliers"
                                           asp-action="Index"
                                           asp-require-permission="suppliers.view">
                                            <i class="fas fa-truck"></i> الموردين
                                        </a>
                                    </li>
                                }
                                @if (canSupplierTypes)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link @(currentController == "suppliertypes" ? "active" : "")"
                                           asp-controller="SupplierTypes"
                                           asp-action="Index"
                                           asp-require-permission="suppliertypes.view">
                                            <i class="fa-solid fa-tags"></i> أنواع الموردين
                                        </a>
                                    </li>
                                }
                            </ul>
                        </li>
                    }
                    @if (canSystemSettings)
                    {
                        <li class="nav-item" data-menu-key="system-settings">
                            <a class="nav-link @(currentController == "systemsettings" ? "active" : "")"
                               asp-controller="SystemSettings"
                               asp-action="Index"
                               asp-require-permission="systemsettings.view">
                                <i class="fas fa-cog"></i> إعدادات النظام
                            </a>
                        </li>
                    }
                    @if (showOperations)
                    {
                        <li class="nav-item" data-menu-key="operations">
                            <a class="nav-link nav-link-toggle d-flex align-items-center justify-content-between" data-bs-toggle="collapse" href="#operationsMenu" data-bs-target="#operationsMenu" role="button" aria-expanded="@(operationsActive ? "true" : "false")" aria-controls="operationsMenu">
                                <span class="nav-link-label"><i class="fas fa-list-alt"></i> العمليات</span>
                                <i class="fas fa-angle-down toggle-icon"></i>
                            </a>
                            <ul class="submenu collapse nav flex-column @(operationsActive ? "show" : "")" id="operationsMenu">
                                @if (canCashClosuresCreate)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="CashBoxClosures"
                                           asp-action="Create"
                                           asp-require-permission="cashclosures.create">
                                            <i class="fas fa-sitemap"></i> جرد الصندوق
                                        </a>
                                    </li>
                                }
                                @if (canTransfersView)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="Transfers"
                                           asp-action="Index"
                                           asp-require-permission="transfers.view">
                                            <i class="fas fa-exchange"></i> حوالاتي
                                        </a>
                                    </li>
                                }

                                @if (canCashClosuresView)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="CashBoxClosures"
                                           asp-action="MyClosures"
                                           asp-require-permission="cashclosures.view">
                                            <i class="fas fa-sitemap"></i> تقرير جرد صندوقي
                                        </a>
                                    </li>

                                }
                                <li class="nav-item">
                                    <a class="nav-link"
                                       asp-controller="Reports"
                                       asp-action="UserDailyJournalEntries"
                                       asp-require-permission="cashclosures.view">
                                        <i class="fas fa-sitemap"></i> قيود الصندوق اليومية
                                    </a>
                                </li>

                                <li class="nav-item">
                                    <a class="nav-link"
                                       asp-controller="Reports"
                                       asp-action="UserAccountJournalEntries"
                                       asp-require-permission="useracc.view">
                                        <i class="fas fa-wallet"></i> حركات حسابات المستخدم
                                    </a>
                                </li>


                                @if (canCashClosuresApprove)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="CashBoxClosures"
                                           asp-action="Pending"
                                           asp-require-permission="cashclosures.approve">
                                            <i class="fas fa-sitemap"></i> طلبات إغلاق الصندوق
                                        </a>
                                    </li>
                                }

                                @if (canCashClosuresReport || canCashClosuresView)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="CashBoxClosures"
                                           asp-action="Report"
                                           asp-require-permission="@(canCashClosuresReport ? "cashclosures.report" : "cashclosures.view")">
                                            <i class="fas fa-file-alt"></i> تقرير إغلاقات الصندوق
                                        </a>
                                    </li>
                                }

                                @if (canTransfersManage)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="Transfers"
                                           asp-action="Manage"
                                           asp-require-permission="transfers.manage">
                                            <i class="fas fa-project-diagram"></i> إدارة الحوالات
                                        </a>
                                    </li>
                                }
                            </ul>
                        </li>
                    }
                    @if (showAssets)
                    {
                        <li class="nav-item" data-menu-key="assets">
                            <a class="nav-link nav-link-toggle d-flex align-items-center justify-content-between" data-bs-toggle="collapse" href="#assetsMenu" data-bs-target="#assetsMenu" role="button" aria-expanded="@(assetsActive ? "true" : "false")" aria-controls="assetsMenu">
                                <span class="nav-link-label"><i class="fas fa-boxes"></i> الاصول الثابته</span>
                                <i class="fas fa-angle-down toggle-icon"></i>
                            </a>
                            <ul class="submenu collapse nav flex-column @(assetsActive ? "show" : "")" id="assetsMenu">
                                @if (canAssets)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="Assets"
                                           asp-action="Index"
                                           asp-require-permission="assets.view">
                                            <i class="fas fa-list"></i> قائمة الأصول الثابته
                                        </a>
                                    </li>
                                }
                                @if (canAssetTypes)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="AssetTypes"
                                           asp-action="Index"
                                           asp-require-permission="assettypes.view">
                                            <i class="fas fa-tags"></i> أنواع الأصول
                                        </a>
                                    </li>
                                }
                                @if (canAssetExpenses)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="AssetExpenses"
                                           asp-action="Index"
                                           asp-require-permission="assetexpenses.view">
                                            <i class="fas fa-money-bill"></i> مصاريف الأصول الثابته
                                        </a>
                                    </li>
                                }
                            </ul>
                        </li>
                    }
                    @if (showVouchers)
                    {
                        <li class="nav-item" data-menu-key="vouchers">
                            <a class="nav-link nav-link-toggle d-flex align-items-center justify-content-between" data-bs-toggle="collapse" href="#vouchersMenu" data-bs-target="#vouchersMenu" role="button" aria-expanded="@(vouchersActive ? "true" : "false")" aria-controls="vouchersMenu">
                                <span class="nav-link-label"><i class="fas fa-file-invoice-dollar"></i> السندات</span>
                                <i class="fas fa-angle-down toggle-icon"></i>
                            </a>
                            <ul class="submenu collapse nav flex-column @(vouchersActive ? "show" : "")" id="vouchersMenu">
                                @if (canTransactionsOverview)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="Transactions"
                                           asp-action="Index">
                                            <i class="fas fa-table-list"></i> كل الحركات
                                        </a>
                                    </li>

                                }
                                <li class="nav-item">
                                    <a class="nav-link"
                                       asp-controller="AccountSettlements"
                                       asp-action="Index" asp-require-permission="accountsettlements.view">
                                        <i class="fa-solid fa-scale-balanced"></i> تسوية حساب مالي
                                    </a>
                                </li>

                                @if (canReceiptVouchers)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="ReceiptVouchers"
                                           asp-action="Index"
                                           asp-require-permission="receiptvouchers.view">
                                            <i class="fas fa-list"></i> سندات القبض
                                        </a>
                                    </li>
                                }
                                @if (canReceiptVouchersCreate)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="ReceiptVouchers"
                                           asp-action="Create"
                                           asp-require-permission="receiptvouchers.create">
                                            <i class="fas fa-plus"></i> إضافة سند قبض
                                        </a>
                                    </li>
                                }
                                @if (canDisbursementVouchers)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="DisbursementVouchers"
                                           asp-action="Index"
                                           asp-require-permission="disbursementvouchers.view">
                                            <i class="fas fa-list"></i> سندات دفع
                                        </a>
                                    </li>
                                }
                                @if (canDisbursementVouchersCreate)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="DisbursementVouchers"
                                           asp-action="Create"
                                           asp-require-permission="disbursementvouchers.create">
                                            <i class="fas fa-plus"></i> إضافة سند دفع
                                        </a>
                                    </li>
                                }
                                @if (canPaymentVouchers)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="PaymentVouchers"
                                           asp-action="Index"
                                           asp-require-permission="paymentvouchers.view">
                                            <i class="fas fa-list"></i> سندات المصاريف
                                        </a>
                                    </li>
                                }
                                @if (canPaymentVouchersCreate)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="PaymentVouchers"
                                           asp-action="Create"
                                           asp-require-permission="paymentvouchers.create">
                                            <i class="fas fa-plus"></i> إضافة سند صرف
                                        </a>
                                    </li>
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="PaymentVouchers"
                                           asp-action="CreateFromAgent"
                                           asp-require-permission="paymentvouchers.create">
                                            <i class="fas fa-user-tie"></i> سند دفع وكيل


                                        </a>
                                    </li>
                                }
                                @if (canWorkflowApprovalsView)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="WorkflowApprovals"
                                           asp-action="Index"
                                           asp-require-permission="workflowapprovals.view">
                                            <i class="fas fa-check-circle"></i> موافقات سندات الدفع
                                        </a>
                                    </li>
                                }
                                @if (canWorkflowDefinitionsManage)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="WorkflowDefinitions"
                                           asp-action="Index"
                                           asp-require-permission="workflowdefinitions.manage">
                                            <i class="fas fa-project-diagram"></i> سير عمل السندات
                                        </a>
                                    </li>
                                }
                                @if (canDynamicScreensManage)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="DynamicScreens"
                                           asp-action="Index"
                                           asp-require-permission="dynamicscreens.manage">
                                            <i class="fas fa-layer-group"></i> إدارة الشاشات الديناميكية
                                        </a>
                                    </li>
                                }
                            </ul>
                        </li>
                    }
                    @await Component.InvokeAsync("DynamicScreensMenu")
                    @if (showAccountManagement)
                    {
                        <li class="nav-item" data-menu-key="account-management">
                            <a class="nav-link nav-link-toggle d-flex align-items-center justify-content-between" data-bs-toggle="collapse" href="#accountMgmtMenu" data-bs-target="#accountMgmtMenu" role="button" aria-expanded="@(accountMgmtActive ? "true" : "false")" aria-controls="accountMgmtMenu">
                                <span class="nav-link-label"><i class="fas fa-briefcase"></i> إدارة الحسابات</span>
                                <i class="fas fa-angle-down toggle-icon"></i>
                            </a>
                            <ul class="submenu collapse nav flex-column @(accountMgmtActive ? "show" : "")" id="accountMgmtMenu">
                                @if (canBusinessStatementBulk)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="AccountManagement"
                                           asp-action="BusinessStatementBulk"
                                           asp-require-permission="accountmanagement.businessstatementbulk">
                                            حساب العميل كلي
                                        </a>
                                    </li>
                                }
                                @if (canBusnissStatment)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="AccountManagement"
                                           asp-action="BusnissStatment"
                                           asp-require-permission="accountmanagement.busnissstatment">
                                            حساب العميل
                                        </a>
                                    </li>
                                }
                                @if (canDriverPayment)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="AccountManagement"
                                           asp-action="DriverPayment"
                                           asp-require-permission="accountmanagement.driverpayment">
                                            دفعات السائق
                                        </a>
                                    </li>
                                }
                                @if (canDriverStatment)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="AccountManagement"
                                           asp-action="DriverStatment"
                                           asp-require-permission="accountmanagement.driverstatment">
                                            حساب السائق
                                        </a>
                                    </li>
                                }
                                @if (canUserPayment)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="AccountManagement"
                                           asp-action="UserPayment"
                                           asp-require-permission="accountmanagement.userpayment">
                                            دفعات العميل
                                        </a>
                                    </li>
                                }
                                @if (canUserPayment)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="AccountManagement"
                                           asp-action="RPTPaymentHistoryUsers"
                                           asp-require-permission="accountmanagement.userpayment">
                                            فواتير العملاء اليومية
                                        </a>
                                    </li>
                                }
                                @if (canDriverStatment)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="AccountManagement"
                                           asp-action="RptpaymentHistoryDrivers"
                                           asp-require-permission="accountmanagement.driverstatment">
                                            فواتير السائق اليومية
                                        </a>
                                    </li>
                                }

                                <li class="nav-item">
                                    <a class="nav-link"
                                       asp-controller="AccountManagement"
                                       asp-action="ReceivePayments"
                                       asp-require-permission="accountmanagement.receivepayments">
                                        تسليم فواتير
                                    </a>
                                </li>

                            </ul>
                        </li>
                    }


                    @if (showJournal)
                    {
                        <li class="nav-item" data-menu-key="journal">
                            <a class="nav-link nav-link-toggle d-flex align-items-center justify-content-between" data-bs-toggle="collapse" href="#journalMenu" data-bs-target="#journalMenu" role="button" aria-expanded="@(journalActive ? "true" : "false")" aria-controls="journalMenu">
                                <span class="nav-link-label"><i class="fas fa-file-invoice"></i> القيود المالية</span>
                                <i class="fas fa-angle-down toggle-icon"></i>
                            </a>
                            <ul class="submenu collapse nav flex-column @(journalActive ? "show" : "")" id="journalMenu">
                                @if (canJournalView)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="JournalEntries"
                                           asp-action="Index"
                                           asp-require-permission="journal.view">
                                            <i class="fas fa-list"></i> قائمة القيود
                                        </a>
                                    </li>
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="JournalEntries"
                                           asp-action="Draft"
                                           asp-require-permission="journal.view">
                                            <i class="fas fa-edit"></i> المسودات
                                        </a>
                                    </li>
                                }
                                @if (canJournalApprove)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="JournalEntries"
                                           asp-action="Posted"
                                           asp-require-permission="journal.approve">
                                            <i class="fas fa-check"></i> المرحلة
                                        </a>
                                    </li>
                                }
                                @if (canJournalCreate)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="JournalEntries"
                                           asp-action="Create"
                                           asp-require-permission="journal.create">
                                            <i class="fas fa-plus"></i> إضافة قيد جديد
                                        </a>
                                    </li>
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="CompoundJournalDefinitions"
                                           asp-action="Index"
                                           asp-require-permission="journal.create">
                                            <i class="fas fa-project-diagram"></i> القيود المركبة
                                        </a>
                                    </li>
                                }
                            </ul>
                        </li>
                    }
                    @if (showHr)
                    {
                        <li class="nav-item" data-menu-key="hr">
                            <a class="nav-link nav-link-toggle d-flex align-items-center justify-content-between" data-bs-toggle="collapse" href="#hrMenu" data-bs-target="#hrMenu" role="button" aria-expanded="@(hrActive ? "true" : "false")" aria-controls="hrMenu">
                                <span class="nav-link-label"><i class="fas fa-users-cog"></i> شؤون الموظفين</span>
                                <i class="fas fa-angle-down toggle-icon"></i>
                            </a>
                            <ul class="submenu collapse nav flex-column @(hrActive ? "show" : "")" id="hrMenu">
                                @if (canEmployeesView)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="Employees"
                                           asp-action="Index"
                                           asp-require-permission="employees.view">
                                            <i class="fas fa-user"></i> إدارة الموظفين
                                        </a>
                                    </li>
                                }
                                @if (canPayrollView)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="Payrolls"
                                           asp-action="Index"
                                           asp-require-permission="payroll.view">
                                            <i class="fas fa-money-check-alt"></i> إدارة الرواتب
                                        </a>
                                    </li>
                                }
                                @if (canPayrollProcess)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="DeductionTypes"
                                           asp-action="Index"
                                           asp-require-permission="payroll.process">
                                            <i class="fas fa-tags"></i> أنواع الخصومات
                                        </a>
                                    </li>
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="AllowanceTypes"
                                           asp-action="Index"
                                           asp-require-permission="payroll.process">
                                            <i class="fas fa-gift"></i> أنواع البدلات
                                        </a>
                                    </li>
                                }
                                @if (canSalaryPaymentsView)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="SalaryPayments"
                                           asp-action="Index"
                                           asp-require-permission="salarypayments.view">
                                            <i class="fas fa-file-invoice-dollar"></i> سندات صرف الرواتب
                                        </a>
                                    </li>
                                }
                                @if (canEmployeeAdvancesView)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="EmployeeAdvances"
                                           asp-action="Index"
                                           asp-require-permission="employeeadvances.view">
                                            <i class="fas fa-hand-holding-usd"></i> سندات صرف السلف
                                        </a>
                                    </li>
                                }
                                @if (canEmployeesEdit)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="EmployeeDeductions"
                                           asp-action="Index"
                                           asp-require-permission="employees.edit">
                                            <i class="fas fa-minus-circle"></i> خصومات الموظفين
                                        </a>
                                    </li>
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="EmployeeAllowances"
                                           asp-action="Index"
                                           asp-require-permission="employees.edit">
                                            <i class="fas fa-plus-circle"></i> بدلات الموظفين
                                        </a>
                                    </li>
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="EmployeeLoans"
                                           asp-action="Index"
                                           asp-require-permission="employees.edit">
                                            <i class="fas fa-landmark"></i> قروض الموظفين
                                        </a>
                                    </li>
                                }
                            </ul>
                        </li>
                    }
                    @if (showReports)
                    {
                        <li class="nav-item" data-menu-key="reports">
                            <a class="nav-link nav-link-toggle d-flex align-items-center justify-content-between" data-bs-toggle="collapse" href="#reportsMenu" data-bs-target="#reportsMenu" role="button" aria-expanded="@(reportsActive ? "true" : "false")" aria-controls="reportsMenu">
                                <span class="nav-link-label"><i class="fas fa-chart-bar"></i> التقارير</span>
                                <i class="fas fa-angle-down toggle-icon"></i>
                            </a>
                            <ul class="submenu collapse nav flex-column @(reportsActive ? "show" : "")" id="reportsMenu">




                                @if (canReportsView)
                                {
                                    <li class="report-item nav-item">
                                        <a class="nav-link"
                                           asp-controller="Reports"
                                           asp-action="INDEX"
                                           asp-require-permission="reports.view">
                                            <i class="fas fa-balance-scale"></i> جميع التقارير
                                        </a>
                                    </li>

                                    <li class="report-item nav-item">
                                        <a class="nav-link"
                                           asp-controller="Reports"
                                           asp-action="TrialBalance"
                                           asp-require-permission="reports.view">
                                            <i class="fas fa-balance-scale"></i> ميزان المراجعة
                                        </a>
                                    </li>
                                    <li class="report-item nav-item">
                                        <a class="nav-link"
                                           asp-controller="Reports"
                                           asp-action="SupplierBalances"
                                           asp-require-permission="reports.view">
                                            <i class="fas fa-truck-loading"></i> أرصدة الموردين
                                        </a>
                                    </li>
                                    <li class="report-item nav-item">
                                        <a class="nav-link"
                                           asp-controller="Reports"
                                           asp-action="BalanceSheet"
                                           asp-require-permission="reports.view">
                                            <i class="fas fa-chart-pie"></i> الميزانية العمومية
                                        </a>
                                    </li>
                                    <li class="report-item nav-item">
                                        <a class="nav-link"
                                           asp-controller="Reports"
                                           asp-action="IncomeStatement"
                                           asp-require-permission="reports.view">
                                            <i class="fas fa-chart-line"></i> قائمة الدخل
                                        </a>
                                    </li>
                                    <li class="report-item"><hr class="dropdown-divider"></li>
                                    <li class="report-item nav-item">
                                        <a class="nav-link"
                                           asp-controller="Reports"
                                           asp-action="GeneralLedger"
                                           asp-require-permission="reports.view">
                                            <i class="fas fa-book"></i> دفتر الأستاذ العام
                                        </a>
                                    </li>
                                    <li class="report-item nav-item">
                                        <a class="nav-link"
                                           asp-controller="Reports"
                                           asp-action="AccountStatement"
                                           asp-require-permission="reports.view">
                                            <i class="fas fa-file-alt"></i> كشف حساب
                                        </a>
                                    </li>
                                    <li class="report-item nav-item">
                                        <a class="nav-link"
                                           asp-controller="Reports"
                                           asp-action="AccountBalanceDiscrepancies"
                                           asp-require-permission="reports.view">
                                            <i class="fas fa-exclamation-triangle"></i> فروقات أرصدة الحسابات
                                        </a>
                                    </li>
                                    @if (hasCashBox)
                                    {
                                        <li class="report-item nav-item">
                                            <a class="nav-link"
                                               asp-controller="Reports"
                                               asp-action="UserDailyJournalEntries"
                                               asp-require-permission="reports.view">
                                                <i class="fas fa-calendar-day"></i> قيود الصندوق اليومية
                                            </a>
                                        </li>
                                    }
                                    <li class="report-item nav-item">
                                        <a class="nav-link"
                                           asp-controller="Reports"
                                           asp-action="BranchExpenses"
                                           asp-require-permission="reports.view">
                                            <i class="fas fa-code-branch"></i> تقرير مصروفات الفروع
                                        </a>
                                    </li>
                                    <li class="report-item nav-item">
                                        <a class="nav-link"
                                           asp-controller="Reports"
                                           asp-action="BranchIncomeStatement"
                                           asp-require-permission="reports.view">
                                            <i class="fas fa-sitemap"></i> قائمة الدخل للفروع
                                        </a>
                                    </li>
                                    <li class="report-item nav-item">
                                        <a class="nav-link"
                                           asp-controller="Reports"
                                           asp-action="BranchPerformanceSummary"
                                           asp-require-permission="reports.view">
                                            <i class="fas fa-network-wired"></i> ملخص أداء الفروع
                                        </a>
                                    </li>
                                }
                                @if (canReportsPending)
                                {
                                    <li class="report-item nav-item">
                                        <a class="nav-link"
                                           asp-controller="Reports"
                                           asp-action="PendingTransactions"
                                           asp-require-permission="reports.pending">
                                            <i class="fas fa-hourglass-half"></i> الحركات غير المرحلة
                                        </a>
                                    </li>
                                }
                                @if (canReportsDynamic)
                                {
                                    <li class="report-item nav-item">
                                        <a class="nav-link"
                                           asp-controller="Reports"
                                           asp-action="DynamicPivot"
                                           asp-require-permission="reports.dynamic">
                                            <i class="fas fa-hourglass-half"></i>انشاء تقريرPivot
                                        </a>
                                    </li>
                                    <li class="report-item nav-item">
                                        <a class="nav-link"
                                           asp-controller="Reports"
                                           asp-action="QueryBuilder"
                                           asp-require-permission="reports.dynamic">
                                            <i class="fas fa-project-diagram"></i> مصمم التقارير
                                        </a>
                                    </li>
                                    <li class="report-item nav-item">
                                        <a class="nav-link"
                                           asp-controller="DynamicRdlcReports"
                                           asp-action="Index"
                                           asp-require-permission="reports.dynamic">
                                            <i class="fa-solid fa-file-circle-plus"></i> تقارير RDLC الديناميكية
                                        </a>
                                    </li>
                                }
                            </ul>
                        </li>
                    }
                    @if (showSettings)
                    {
                        <li class="nav-item" data-menu-key="settings">
                            <a class="nav-link nav-link-toggle d-flex align-items-center justify-content-between" data-bs-toggle="collapse" href="#settingsMenu" data-bs-target="#settingsMenu" role="button" aria-expanded="@(settingsActive ? "true" : "false")" aria-controls="settingsMenu">
                                <span class="nav-link-label"><i class="fas fa-cog"></i> الإعدادات</span>
                                <i class="fas fa-angle-down toggle-icon"></i>
                            </a>
                            <ul class="submenu collapse nav flex-column @(settingsActive ? "show" : "")" id="settingsMenu">
                                @if (canBranches)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="Branches"
                                           asp-action="Index"
                                           asp-require-permission="branches.view">
                                            <i class="fas fa-building"></i> الفروع
                                        </a>
                                    </li>
                                }
                                @if (canCostCenters)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="CostCenters"
                                           asp-action="Index"
                                           asp-require-permission="costcenters.view">
                                            <i class="fas fa-bullseye"></i> مراكز التكلفة
                                        </a>
                                    </li>
                                }
                                @if (canCurrencies)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="Currencies"
                                           asp-action="Index"
                                           asp-require-permission="currencies.view">
                                            <i class="fas fa-money-bill-wave"></i> العملات
                                        </a>
                                    </li>
                                }
                                @if (canPermissionGroups)
                                {
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="PermissionGroups"
                                           asp-action="Index"
                                           asp-require-permission="permissiongroups.view">
                                            <i class="fas fa-key"></i> مجموعات الصلاحيات
                                        </a>
                                    </li>
                                }
                                @if (canUsers)
                                {
                                    @if (canBranches || canCostCenters || canCurrencies || canPermissionGroups)
                                    {
                                        <li><hr class="dropdown-divider"></li>
                                    }
                                    <li class="nav-item">
                                        <a class="nav-link"
                                           asp-controller="Users"
                                           asp-action="Index"
                                           asp-require-permission="users.view">
                                            <i class="fas fa-users"></i> المستخدمون
                                        </a>
                                    </li>
                                }
                            </ul>
                        </li>
                    }
                </ul>
            </div>
        </nav>
        <div id="content" class="flex-fill">
            <header class="top-navbar shadow-lg">
                <div class="container-fluid d-flex align-items-center flex-wrap gap-3">
                    <div class="d-flex align-items-center gap-3 flex-grow-1 flex-lg-grow-0">
                        <button id="sidebarToggle" class="btn btn-outline-light btn-icon" type="button" aria-label="تبديل القائمة الجانبية">
                            <i class="fas fa-bars"></i>
                        </button>
                        <a class="navbar-brand d-flex align-items-center text-white" href="@Url.Action("Index", "Dashboard")">
                            <span class="brand-icon">
                                <i class="fa-solid fa-chart-line"></i>
                            </span>
                            <span class="brand-text">
                                <span class="brand-title">RoadFinity</span>
                                <span class="brand-subtitle"> ERP System  </span>
                            </span>
                        </a>
                    </div>

                    <ul class="navbar-nav ms-auto align-items-center flex-row flex-wrap gap-2 top-navbar-actions order-2 order-lg-3">
                        @if (showQuickAccountStatement)
                        {
                            <li class="nav-item">
                                <a class="btn btn-outline-primary d-none d-md-inline-flex align-items-center"
                                   asp-controller="Reports"
                                   asp-action="AccountStatement"
                                   asp-require-permission="@(isSuperAdmin && canReportsView ? "reports.view" : "reports.quickaccountstatement")">
                                    <i class="fa-solid fa-file-lines me-2"></i>
                                    كشف حساب مباشر
                                </a>
                            </li>
                        }
                        @await Component.InvokeAsync("UserAccountBalances")
                        <li class="nav-item">
                            @await Component.InvokeAsync("Notifications", new { category = NotificationCategories.Workflow })
                        </li>
                        <li class="nav-item">
                            @await Component.InvokeAsync("Notifications", new { category = NotificationCategories.Login })
                        </li>
                        @if (User.Identity.IsAuthenticated)
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    <span class="user-avatar"><i class="fas fa-user"></i></span>
                                    <span class="user-name">@User.Identity.Name</span>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li>
                                        <a class="dropdown-item" href="@Url.Action("Profile", "Account")"><i class="fas fa-user-edit"></i> الملف الشخصي</a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" href="@Url.Action("Sessions", "Account")"><i class="fas fa-satellite-dish"></i> الجلسات النشطة</a>
                                    </li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                                            <button type="submit" class="dropdown-item"><i class="fas fa-sign-out-alt"></i> تسجيل الخروج</button>
                                        </form>
                                    </li>
                                </ul>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="@Url.Action("Login", "Account")"><i class="fas fa-sign-in-alt"></i> تسجيل الدخول</a>
                            </li>
                        }
                    </ul>
                </div>
            </header>

            <!-- Main Content -->
            <main class="page-content">
                <!-- Breadcrumb -->
                <nav aria-label="breadcrumb" class="mb-3">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item">
                            <a href="@Url.Action("Index", "Dashboard")">الرئيسية</a>
                        </li>
                        @if (ViewData["Breadcrumb"] != null)
                        {
                            @Html.Raw(ViewData["Breadcrumb"])
                        }
                        else
                        {
                            <li class="breadcrumb-item active">@ViewData["Title"]</li>
                        }
                    </ol>
                </nav>

                <!-- Alerts -->
                @if (TempData["SuccessMessage"] != null)
                {
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        <i class="fas fa-check-circle"></i> @TempData["SuccessMessage"]
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                }

                @if (TempData["ErrorMessage"] != null)
                {
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <i class="fas fa-exclamation-circle"></i> @TempData["ErrorMessage"]
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                }

                @if (TempData["InfoMessage"] != null)
                {
                    <div class="alert alert-info alert-dismissible fade show" role="alert">
                        <i class="fas fa-info-circle"></i> @TempData["InfoMessage"]
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                }

                @RenderBody()
            </main>

            <!-- Footer -->
            <footer class="app-footer text-center py-3 mt-5">
                <div class="container">
                    <p class="text-muted mb-0">
                        &copy; @DateTime.Now.Year - RoadFinity  - جميع الحقوق محفوظة
                    </p>
                </div>
            </footer>
        </div>
    </div>
    <div class="modal fade" id="detailsModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-lg modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">تفاصيل الدفعة</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body" id="detailsContent"></div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">إغلاق</button>
                </div>
            </div>
        </div>
    </div>

    <div class="payroll-overlay d-none" id="loadingOverlay">
        <div class="overlay-content text-center">
            <div class="spinner-border text-primary mb-3" role="status"></div>
            <div class="progress" style="height: 6px;">
                <div class="progress-bar progress-bar-striped progress-bar-animated" role="progressbar" style="width: 100%"></div>
            </div>
            <p class="mt-2">جاري معالجة الطلب...</p>
        </div>
    </div>
    <div class="modal fade" id="summaryModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-lg modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">مراجعة تفاصيل التنزيل</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div id="summaryContent"></div>
                    <div class="alert alert-secondary mt-3" id="historyNotice">
                        <i class="fas fa-history"></i> تم حفظ هذه الدفعة في سجل الرواتب ويمكن الرجوع إليها قبل التأكيد.
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">رجوع</button>
                    <button type="button" class="btn btn-warning" id="holdPayrollBtn" asp-require-permission="payroll.process">
                        <i class="fas fa-pause"></i> تعليق الدفعة
                    </button>
                    <button type="button" class="btn btn-success" id="confirmPayrollBtn" asp-require-permission="payroll.process">
                        <span class="spinner-border spinner-border-sm d-none" role="status" aria-hidden="true"></span>
                        تأكيد التنزيل
                    </button>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="allowanceModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-lg modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="allowanceModalTitle">البدلات</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="إغلاق"></button>
                </div>
                <div class="modal-body">
                    <div id="allowanceEmptyState" class="alert alert-info text-center">
                        لا توجد بدلات لهذا الموظف.
                    </div>
                    <div class="table-responsive">
                        <table class="table table-sm align-middle d-none" id="allowanceTable">
                            <thead class="table-light">
                                <tr>
                                    <th>نوع البدل</th>
                                    <th>الوصف</th>
                                    <th class="text-end">المبلغ</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody id="allowanceTableBody"></tbody>
                        </table>
                    </div>
                    <button type="button" class="btn btn-outline-primary btn-sm mt-3" id="addAllowanceBtn">
                        <i class="fas fa-plus"></i> إضافة بدل
                    </button>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">إغلاق</button>
                    <button type="button" class="btn btn-primary" id="saveAllowanceBtn">حفظ</button>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="deductionModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-lg modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="deductionModalTitle">الخصومات</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="إغلاق"></button>
                </div>
                <div class="modal-body">
                    <div id="deductionEmptyState" class="alert alert-info text-center">
                        لا توجد خصومات لهذا الموظف.
                    </div>
                    <div class="table-responsive">
                        <table class="table table-sm align-middle d-none" id="deductionTable">
                            <thead class="table-light">
                                <tr>
                                    <th>نوع الخصم</th>
                                    <th>الوصف</th>
                                    <th class="text-end">المبلغ</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody id="deductionTableBody"></tbody>
                        </table>
                    </div>
                    <button type="button" class="btn btn-outline-primary btn-sm mt-3" id="addDeductionBtn">
                        <i class="fas fa-plus"></i> إضافة خصم
                    </button>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">إغلاق</button>
                    <button type="button" class="btn btn-primary" id="saveDeductionBtn">حفظ</button>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="sellAssetModal" tabindex="-1" aria-labelledby="sellAssetModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <form asp-action="Sell" method="post">
                    @Html.AntiForgeryToken()
                    <div class="modal-header">
                        <h5 class="modal-title" id="sellAssetModalLabel">بيع الأصل</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <input type="hidden" name="id" id="sellAssetId" />
                        <div class="mb-3">
                            <label class="form-label">الأصل</label>
                            <div id="sellAssetName" class="form-control-plaintext fw-bold"></div>
                        </div>
                        <div class="mb-3">
                            <label for="salePriceInput" class="form-label">قيمة البيع (القيمة السوقية)</label>
                            <input type="number" class="form-control" id="salePriceInput" name="salePrice" min="0" step="0.01" required />
                        </div>
                        <div class="alert alert-secondary" id="sellAssetSummary">
                            <div>القيمة الدفترية الحالية: <span id="sellAssetBookValue"></span></div>
                            <div class="mt-2">الربح / الخسارة المتوقعة: <span id="sellAssetProfitLoss" class="fw-bold"></span></div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">إلغاء</button>
                        <button type="submit" class="btn btn-primary">بيع الأصل</button>
                    </div>
                </form>
            </div>
        </div>
    </div>


    <div id="notificationToastContainer" class="toast-container position-fixed bottom-0 start-50 translate-middle-x p-3"></div>

    <div class="modal fade" id="accountModal" tabindex="-1">
        <div class="modal-dialog modal-lg">
            <div class="modal-content"></div>
        </div>
    </div>


    <div class="modal fade" id="paymentVoucherConfirmModal" tabindex="-1" aria-labelledby="paymentVoucherConfirmLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="paymentVoucherConfirmLabel">تأكيد إضافة الحركة</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="إغلاق"></button>
                </div>
                <div class="modal-body">
                    <p class="mb-3">يرجى مراجعة ملخص السند قبل الحفظ:</p>
                    <ul class="list-group" id="paymentVoucherSummary"></ul>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">عودة للتعديل</button>
                    <button type="button" class="btn btn-primary" id="paymentVoucherConfirmBtn">تأكيد الحفظ</button>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="paymentVoucherAgentConfirmModal" tabindex="-1" aria-labelledby="paymentVoucherAgentConfirmLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="paymentVoucherAgentConfirmLabel">تأكيد إضافة الحركة</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="إغلاق"></button>
                </div>
                <div class="modal-body">
                    <p class="mb-3">يرجى مراجعة ملخص السند قبل الحفظ:</p>
                    <ul class="list-group" id="paymentVoucherAgentSummary"></ul>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">عودة للتعديل</button>
                    <button type="button" class="btn btn-primary" id="paymentVoucherAgentConfirmBtn">تأكيد الحفظ</button>
                </div>
            </div>
        </div>
    </div>


    <div class="modal fade" id="receiptVoucherConfirmModal" tabindex="-1" aria-labelledby="receiptVoucherConfirmLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="receiptVoucherConfirmLabel">تأكيد إضافة الحركة</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="إغلاق"></button>
                </div>
                <div class="modal-body">
                    <p class="mb-3">يرجى مراجعة ملخص السند قبل الحفظ:</p>
                    <ul class="list-group" id="receiptVoucherSummary"></ul>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">عودة للتعديل</button>
                    <button type="button" class="btn btn-primary" id="receiptVoucherConfirmBtn">تأكيد الحفظ</button>
                </div>
            </div>
        </div>
    </div>
    <div class="modal fade" id="assetExpenseConfirmModal" tabindex="-1" aria-labelledby="assetExpenseConfirmLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="assetExpenseConfirmLabel">تأكيد إضافة الحركة</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="إغلاق"></button>
                </div>
                <div class="modal-body">
                    <p class="mb-3">يرجى مراجعة ملخص الحركة قبل الحفظ:</p>
                    <ul class="list-group" id="assetExpenseSummary"></ul>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">عودة للتعديل</button>
                    <button type="button" class="btn btn-primary" id="assetExpenseConfirmBtn">تأكيد الحفظ</button>
                </div>
            </div>
        </div>
    </div>
    <!-- Scripts -->
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.0/Sortable.min.js"></script>
    @*     <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
 *@

    <script src="~/js/sweetalert.min.js"></script>
    <script src="~/js/select2.min.js"></script>

    <script src="~/js/site.js" asp-append-version="true"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/7.0.12/signalr.min.js"></script>
    <script src="~/js/notifications.js" asp-append-version="true"></script>

    <!-- Loading Overlay -->
    <div id="loadingOverlay" class="loading-overlay" style="display: none;">
        <div class="loading-spinner">
            <i class="fas fa-spinner fa-spin fa-3x"></i>
            <p class="mt-3">جاري التحميل...</p>
        </div>
    </div>

    @await RenderSectionAsync("Scripts", required: false)

    <script>
        // Global functions
        function showLoading() {
            $('#loadingOverlay').show();
        }

        function hideLoading() {
            $('#loadingOverlay').hide();
        }

        // Auto-hide alerts after 5 seconds
        setTimeout(function() {
            $('.alert').fadeOut();
        }, 5000);

        // AJAX setup for CSRF token
        $.ajaxSetup({
            beforeSend: function(xhr, settings) {
                if (settings.type === 'POST' || settings.type === 'PUT' || settings.type === 'DELETE') {
                    xhr.setRequestHeader("RequestVerificationToken",
                        $('input[name="__RequestVerificationToken"]').val());
                }
            }
        });

        // Filter account options based on selected currency in voucher forms
        document.addEventListener('DOMContentLoaded', function () {
            var currencySelect = document.querySelector('select[name="CurrencyId"]');
            var accountSelect = document.querySelector('select[name="AccountId"]');
            var sidebarMenu = document.getElementById('sidebarMenu');
            var incomingTransfersModalElement = document.getElementById('incomingTransfersModal');

            if (incomingTransfersModalElement && window.bootstrap) {
                var incomingTransfersModal = bootstrap.Modal.getOrCreateInstance(incomingTransfersModalElement);
                incomingTransfersModal.show();
            }
            if (currencySelect && accountSelect) {
                function filterAccounts() {
                    var currency = currencySelect.value;
                    Array.from(accountSelect.options).forEach(function (opt) {
                        opt.hidden = opt.getAttribute('data-currency') !== currency;
                    });
                    var firstVisible = Array.from(accountSelect.options).find(function (opt) { return !opt.hidden; });
                    if (firstVisible) accountSelect.value = firstVisible.value;
                }
                filterAccounts();
                currencySelect.addEventListener('change', filterAccounts);
            }

            if (sidebarMenu) {
                var menuOrderUrl = '@Url.Content("~/api/UserPreferences/sidebar-menu-order")';
                var saveTimeoutId = null;

                function getCurrentOrder() {
                    return Array.from(sidebarMenu.querySelectorAll(':scope > li[data-menu-key]'))
                        .map(function (item) { return item.getAttribute('data-menu-key'); });
                }

                function applyMenuOrder(order) {
                    if (!Array.isArray(order) || !order.length) {
                        return;
                    }

                    var items = Array.from(sidebarMenu.querySelectorAll(':scope > li[data-menu-key]'));
                    var remaining = {};

                    items.forEach(function (item) {
                        var key = item.getAttribute('data-menu-key');
                        if (key) {
                            remaining[key] = item;
                        }
                    });

                    var orderedItems = [];

                    order.forEach(function (key) {
                        if (remaining[key]) {
                            orderedItems.push(remaining[key]);
                            delete remaining[key];
                        }
                    });

                    Object.keys(remaining).forEach(function (key) {
                        orderedItems.push(remaining[key]);
                    });

                    orderedItems.forEach(function (item) {
                        sidebarMenu.appendChild(item);
                    });
                }

                function loadMenuOrder() {
                    return fetch(menuOrderUrl, { credentials: 'same-origin' })
                        .then(function (response) {
                            if (!response || !response.ok) {
                                return null;
                            }
                            return response.json();
                        })
                        .then(function (data) {
                            if (data && Array.isArray(data.order)) {
                                applyMenuOrder(data.order);
                            }
                        })
                        .catch(function (error) {
                            console.error('Failed to load sidebar menu order', error);
                        });
                }

                function saveMenuOrder() {
                    var payload = {
                        order: getCurrentOrder()
                    };

                    fetch(menuOrderUrl, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        credentials: 'same-origin',
                        body: JSON.stringify(payload)
                    }).catch(function (error) {
                        console.error('Failed to save sidebar menu order', error);
                    });
                }

                function scheduleMenuSave() {
                    if (saveTimeoutId) {
                        clearTimeout(saveTimeoutId);
                    }

                    saveTimeoutId = setTimeout(function () {
                        saveMenuOrder();
                    }, 600);
                }

                function initializeSortable() {
                    if (!window.Sortable) {
                        console.warn('SortableJS is not available; sidebar menu reordering is disabled.');
                        return;
                    }

                    new Sortable(sidebarMenu, {
                        animation: 150,
                        handle: '.nav-link',
                        draggable: 'li[data-menu-key]',
                        onEnd: function () {
                            scheduleMenuSave();
                        }
                    });
                }

                loadMenuOrder().then(function () {
                    initializeSortable();
                });
            }

            var menuSearchInput = document.getElementById('menuSearch');
            if (!menuSearchInput) {
                return;
            }

            var topLevelItems = Array.from(document.querySelectorAll('#sidebarMenu > li.nav-item'));
            var collapseElements = Array.from(document.querySelectorAll('#sidebarMenu .collapse'));
            var hasBootstrap = window.bootstrap && window.bootstrap.Collapse;
            var collapseInstances = new Map();
            var initialStates = new Map();

            function findToggle(id) {
                return document.querySelector('[data-bs-toggle="collapse"][href="#' + id + '"]') ||
                    document.querySelector('[data-bs-toggle="collapse"][data-bs-target="#' + id + '"]');
            }

            collapseElements.forEach(function (collapseEl) {
                initialStates.set(collapseEl.id, collapseEl.classList.contains('show'));
                if (hasBootstrap) {
                    var instance = window.bootstrap.Collapse.getOrCreateInstance(collapseEl, { toggle: false });
                    collapseInstances.set(collapseEl.id, instance);

                    collapseEl.addEventListener('shown.bs.collapse', function () {
                        var toggle = findToggle(collapseEl.id);
                        if (toggle) {
                            toggle.classList.add('open');
                        }
                    });

                    collapseEl.addEventListener('hidden.bs.collapse', function () {
                        var toggle = findToggle(collapseEl.id);
                        if (toggle) {
                            toggle.classList.remove('open');
                        }
                    });
                }

                if (collapseEl.classList.contains('show')) {
                    var initialToggle = findToggle(collapseEl.id);
                    if (initialToggle) {
                        initialToggle.classList.add('open');
                    }
                }
            });

            menuSearchInput.addEventListener('input', function () {
                var query = this.value.trim().toLowerCase();

                if (!query) {
                    topLevelItems.forEach(function (item) {
                        item.style.display = '';
                        var collapseEl = item.querySelector(':scope > .collapse');
                        if (!collapseEl) {
                            return;
                        }

                        Array.from(collapseEl.querySelectorAll(':scope > li')).forEach(function (child) {
                            child.style.display = '';
                        });

                        var shouldShow = initialStates.get(collapseEl.id);
                        if (hasBootstrap && collapseInstances.get(collapseEl.id)) {
                            if (shouldShow) {
                                collapseInstances.get(collapseEl.id).show();
                            } else {
                                collapseInstances.get(collapseEl.id).hide();
                            }
                        } else {
                            collapseEl.classList.toggle('show', !!shouldShow);
                            var toggle = findToggle(collapseEl.id);
                            if (toggle) {
                                toggle.classList.toggle('open', !!shouldShow);
                                toggle.setAttribute('aria-expanded', shouldShow ? 'true' : 'false');
                            }
                        }
                    });
                    return;
                }

                topLevelItems.forEach(function (item) {
                    var collapseEl = item.querySelector(':scope > .collapse');
                    var toggle = item.querySelector(':scope > a.nav-link');
                    var directText = toggle ? toggle.textContent.trim().toLowerCase() : '';
                    var directMatch = directText.indexOf(query) > -1;
                    var itemMatch = directMatch;

                    if (collapseEl) {
                        var collapseId = collapseEl.id;
                        var collapseInstance = collapseInstances.get(collapseId);
                        var childMatch = false;

                        Array.from(collapseEl.querySelectorAll(':scope > li')).forEach(function (child) {
                            var childLink = child.querySelector('a.nav-link');
                            if (!childLink) {
                                child.style.display = directMatch ? '' : 'none';
                                return;
                            }

                            var childText = childLink.textContent.trim().toLowerCase();
                            var isMatch = childText.indexOf(query) > -1;
                            child.style.display = directMatch || isMatch ? '' : 'none';
                            if (isMatch) {
                                childMatch = true;
                            }
                        });

                        itemMatch = itemMatch || childMatch;

                        if (hasBootstrap && collapseInstance) {
                            if (itemMatch) {
                                collapseInstance.show();
                            } else {
                                collapseInstance.hide();
                            }
                        } else {
                            collapseEl.classList.toggle('show', itemMatch);
                            var collapseToggle = findToggle(collapseId);
                            if (collapseToggle) {
                                collapseToggle.classList.toggle('open', itemMatch);
                                collapseToggle.setAttribute('aria-expanded', itemMatch ? 'true' : 'false');
                            }
                        }
                    }

                    item.style.display = itemMatch ? '' : 'none';
                });
            });
        });
    </script>
    <script>
        var alertObj;
        function alertcreated() {
            alertObj = this;
        }
        function alertclose(e) {
            if (e.toastContainer.childElementCount === 0) {
            }
        }
        //function setZoom(zoomLevel) {
        //    document.body.style.zoom = zoomLevel;
        //}
        //// Set zoom to 80%
        //setZoom(0.9);
    </script>

    @if (TempData["TransferPrintUrl"] is string transferPrintUrl && !string.IsNullOrWhiteSpace(transferPrintUrl))
    {
        <script>
            (function () {
                var printUrl = @Html.Raw(JsonSerializer.Serialize(transferPrintUrl));
                if (printUrl) {
                    window.open(printUrl, '_blank');
                }
            })();
        </script>
    }
    <ejs-scripts></ejs-scripts>

</body>
</html>

